import { createStore } from 'vuex'
import structure from './structure.js'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    structure
  }
})
